{
  "name": "neighbor-know-jwt-circuit",
  "version": "0.1.0",
  "private": true,
  "description": "ZK circuit for JWT @usc.edu auth; outputs Poseidon(sub, pepper) nullifier",
  "scripts": {
    "build": "bash scripts/build.sh",
    "setup": "bash scripts/setup.sh",
    "test": "bash scripts/test.sh",
    "copy-to-frontend": "bash scripts/copy-to-frontend.sh"
  },
  "devDependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.4"
  }
}
